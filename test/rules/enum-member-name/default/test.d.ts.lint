enum GlobalLetterCase {
  PascalCase,
  camelCase
  ~~~~~~~~~  [Enum members must be in PascalCase]
}

declare module "some-module" {

  export enum ModuleLetterCase {
    PascalCase,
    camelCase
    ~~~~~~~~~  [Enum members must be in PascalCase]
  }

  export namespace NamespaceA {
    namespace NamespaceB {
      enum NestedModuleLetterCase {
        PascalCase,
        camelCase
        ~~~~~~~~~  [Enum members must be in PascalCase]
      }
    }
  }

}
